version: 1

formatters:
    standard:
        format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"

handlers:
    file_handler:
        class: logging.FileHandler
        level: DEBUG
        formatter: standard
        filename: /Users/mas5mk/Work/Flight-tracker/flask_app/log_.log
        mode: w+

loggers:
    main:
        level: INFO
        handlers: [file_handler]

    get_states:
        level: INFO

root:
    level: INFO
    handlers: [file_handler]
